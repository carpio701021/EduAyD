extends ../base

block scripts
	script(language="javascript",src='/javascript/ingresar_notas.js')

block content
	br
	div.container
		h1 Ingresar Notas
		div
			b Curso: 
			select(name="select_curso",id="select_curso",onchange="seleccionar_curso();")
				option(value='-1', disabled selected) Seleccione un curso
				each cur in cursos
					option(value='#{cur.id_curso}') #{cur.nombre_curso}	
			br
			br
		//form(action='/maestros/ingresar_notas', method='POST')	
		div(id='tabla_alumnos_asignaturas')

